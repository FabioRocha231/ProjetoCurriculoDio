import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    marginTop: 10,
    alignItems: 'center',
    alignSelf: 'center',
    width: '60%',
    backgroundColor: '#fff',
    borderRadius: 5,
    borderWidth: 2,
    borderColor: '#939393',
  },
});
